---
driver:
  name: vagrant

provisioner:
  name: chef_zero
  deprecations_as_errors: true

platforms:
  - name: debian-11
  - name: debian-12
  - name: ubuntu-22.04

suites:
  - name: default
    run_list:
      - recipe[test::default]
